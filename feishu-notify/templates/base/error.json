{
  "_comment": "ERROR 级别默认模板 - 错误通知",
  "elements": [
    {
      "tag": "div",
      "fields": [
        { "key": "任务名称", "value": "{{ task_name }}" },
        { "key": "来源系统", "value": "{{ source }}" },
        { "key": "开始时间", "value": "{{ start_time or timestamp }}" },
        { "key": "失败时间", "value": "{{ end_time or timestamp }}" }
      ]
    },
    {
      "tag": "error_block",
      "condition": "{{ error_msg or content }}"
    },
    {
      "tag": "metrics_block",
      "condition": "{{ metrics }}"
    },
    {
      "tag": "actions",
      "condition": "{{ links }}"
    }
  ],
  "footer_note": null
}

